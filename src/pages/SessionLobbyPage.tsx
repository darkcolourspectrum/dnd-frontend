import React from 'react';
import SessionLobby from '../components/SessionLobby/SessionLobby';

const SessionLobbyPage: React.FC = () => {
  return (
    <div className="session-lobby-page">
      <SessionLobby />
    </div>
  );
};

export default SessionLobbyPage;